# Containers

```
cp v /usr/local/bin/
```

```
export DOCKER_BUILDKIT=0
export COMPOSE_DOCKER_CLI_BUILD=0
docker build -t dujiaju/container container/
docker build -t dujiaju/container-cpp container-cpp/
docker build -t dujiaju/container-go container-go/
docker build -t dujiaju/container-haskell container-haskell/
docker build -t dujiaju/container-java container-java/
docker build -t dujiaju/container-latex container-latex/
docker build -t dujiaju/container-lua container-lua/
docker build -t dujiaju/container-node container-node/
docker build -t dujiaju/container-php container-php/
docker build -t dujiaju/container-python container-python/
docker build -t dujiaju/container-rust container-rust/
docker build -t dujiaju/container-scala container-scala/
docker build -t dujiaju/container-tool container-tool/
docker build -t dujiaju/container-virtual container-virtual/
```